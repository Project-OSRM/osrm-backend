// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Lane } from '../../../../osrm/engine/api/fbresult/lane.js';
import { Position } from '../../../../osrm/engine/api/fbresult/position.js';


export class Intersection {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Intersection {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIntersection(bb:flatbuffers.ByteBuffer, obj?:Intersection):Intersection {
  return (obj || new Intersection()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIntersection(bb:flatbuffers.ByteBuffer, obj?:Intersection):Intersection {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Intersection()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

location(obj?:Position):Position|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Position()).__init(this.bb_pos + offset, this.bb!) : null;
}

bearings(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt16(this.bb!.__vector(this.bb_pos + offset) + index * 2) : 0;
}

bearingsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

bearingsArray():Int16Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Int16Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

classes(index: number):string
classes(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
classes(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

classesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

entry(index: number):boolean|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb!.__vector(this.bb_pos + offset) + index) : false;
}

entryLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

entryArray():Int8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Int8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

inBearing():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

outBearing():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

lanes(index: number, obj?:Lane):Lane|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new Lane()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

lanesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startIntersection(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addLocation(builder:flatbuffers.Builder, locationOffset:flatbuffers.Offset) {
  builder.addFieldStruct(0, locationOffset, 0);
}

static addBearings(builder:flatbuffers.Builder, bearingsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, bearingsOffset, 0);
}

static createBearingsVector(builder:flatbuffers.Builder, data:number[]|Int16Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createBearingsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createBearingsVector(builder:flatbuffers.Builder, data:number[]|Int16Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(2, data.length, 2);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt16(data[i]!);
  }
  return builder.endVector();
}

static startBearingsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(2, numElems, 2);
}

static addClasses(builder:flatbuffers.Builder, classesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, classesOffset, 0);
}

static createClassesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startClassesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEntry(builder:flatbuffers.Builder, entryOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, entryOffset, 0);
}

static createEntryVector(builder:flatbuffers.Builder, data:boolean[]):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(+data[i]!);
  }
  return builder.endVector();
}

static startEntryVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addInBearing(builder:flatbuffers.Builder, inBearing:number) {
  builder.addFieldInt32(4, inBearing, 0);
}

static addOutBearing(builder:flatbuffers.Builder, outBearing:number) {
  builder.addFieldInt32(5, outBearing, 0);
}

static addLanes(builder:flatbuffers.Builder, lanesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, lanesOffset, 0);
}

static createLanesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLanesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endIntersection(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIntersection(builder:flatbuffers.Builder, locationOffset:flatbuffers.Offset, bearingsOffset:flatbuffers.Offset, classesOffset:flatbuffers.Offset, entryOffset:flatbuffers.Offset, inBearing:number, outBearing:number, lanesOffset:flatbuffers.Offset):flatbuffers.Offset {
  Intersection.startIntersection(builder);
  Intersection.addLocation(builder, locationOffset);
  Intersection.addBearings(builder, bearingsOffset);
  Intersection.addClasses(builder, classesOffset);
  Intersection.addEntry(builder, entryOffset);
  Intersection.addInBearing(builder, inBearing);
  Intersection.addOutBearing(builder, outBearing);
  Intersection.addLanes(builder, lanesOffset);
  return Intersection.endIntersection(builder);
}
}
